/**
 * Validates if email domain is valid TLD
 */
export declare function isValidEmailDomain(emailOrDomain: string): boolean;
/**
 * Validates email address format using RFC-compliant regex
 * @param emailAddress - The email address to validate
 * @returns true if email format is valid
 */
export declare function isValidEmail(emailAddress: string): boolean;
